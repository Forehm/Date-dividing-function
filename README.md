# Date-dividing-function
Divides the count of days into years, month etc... This function (DateDivide) takes the count of days as a parameter; After its work it returns a string with de description of dividing;
